import type { Metadata } from 'next' import './globals.css'  export const metadata: Metadata = {   title: 'DocFlow — Free Online PDF Tools',   description: 'Convert, compress, merge, and edit PDFs free online. No sign up. Works in your browser.',   keywords: 'PDF converter, PDF to Word, merge PDF, compress PDF, free PDF tools', }  export default function RootLayout({ children }: { children: React.ReactNode }) {   return (     <html lang="en">       <body>{children}</body>body>     </html>html>   ) }</html>
